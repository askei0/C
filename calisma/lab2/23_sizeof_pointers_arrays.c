// Earlier we listed the size in bytes of different basic types.
// Determine the same for pointers pointing to these types and arrays
// containing elements of this type.

#include <stdio.h>

int main() {
    return 0;
}
