// Determine whether two integers are amicable.
// Two integers are amicable if the sum of the divisors of one (smaller than
// itself) is equal to the other, and vice versa.
// E.g.: 220 and 284 are amicable numbers.

#include <stdio.h>

int main() {
    return 0;
}
