// Try to assign a floating point number, a char, a boolean and a string to
// an int variable. If the assignment is successful then print its value.
// Use -W, -Wall, and -Wextra flags during compilation.
//
// Based on the previous example examine the conversion between all known base
// types. Can all types be converted to another? If yes, what is the rule of
// conversion?

#include <stdio.h>

int main() {
    return 0;
}
